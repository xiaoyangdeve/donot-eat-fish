# 治理实施记录

## 2026-07-27：全站文章标签治理

- 只调整 35 篇普通文章 Frontmatter 中的 `tags`，未修改标题、正文、日期、目录、分类或 permalink。
- 将标签统一为核心主题、技术或方法、关键概念三类，每篇控制在 2～4 个。
- 补齐原有 18 篇空标签文章，治理后空标签为 0，重复标签为 0。
- 移除 `技术系列`、`基本原理`、`大数据` 等低区分度标签，统一 `AB实验`、`Agile`、`FastText` 等同义或大小写不一致的表达。
- 新增 `TAG_TAXONOMY.md`，记录受控词表、命名规范、同义词映射和逐篇标签基线。

### 验收证据

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 58 个 Markdown、35 篇普通文章、89 个受控标签、167 个图片引用；0 警告、0 错误 |
| 标签约束 | 35 篇文章均为 2～4 个标签；空值、重复值、未登记词均为 0 |
| `npm run build` | 客户端和服务端编译成功，生成 59 个 HTML |
| `npm run check:dist` | 检查 6,738 个站内构建引用，0 错误 |
| 标签页 | 源码与生产标签页均为 89 个标签，缺失 0；废弃标签残留 0 |

### 本轮边界

- 本轮不处理历史分类错误；例如 `JVM垃圾回收` 的 `categories` 仍需在后续分类治理中修复。
- 未执行 commit、push、deploy、百度推送或 npm 发布。

## 2026-07-27：项目管理知识库迁移

- 从下载目录导入 3 篇项目管理知识文档：1 篇敏捷体系文章、2 篇预测型项目体系文章。
- 敏捷文章归入“原则与思维”；跨过程组的 PMI 文章位于预测型项目根目录，并使用 06、07 编号避开五大过程组目录编号。
- 迁入 8 张 PNG 附件，统一存放在 `img/project-management/agile` 和 `img/project-management/predictive`。
- 修复全部图片引用，补齐 Frontmatter 描述、分类和标签，并在预测型、敏捷概览页增加体系资料入口。
- 下载目录中的源文件继续保留；两个 `.DS_Store` 未迁入项目。

### 验收证据

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 58 个 Markdown、35 篇普通文章、167 个图片引用；0 错误、18 个空标签警告 |
| `npm run build` | 客户端和服务端编译成功，生成 59 个 HTML |
| `npm run check:dist` | 检查 6,574 个站内构建引用，0 错误 |
| 内容完整性 | 3 篇新文章均进入 sitemap，57 个 permalink 无重复 |
| 附件完整性 | 8 张项目管理图片均进入生产构建，Markdown 无下载目录或临时附件路径残留 |

### 本轮边界

- 未执行 commit、push、deploy、百度推送或 npm 发布。

## 2026-07-27：首页快捷导航布局

- 将桌面端四个首页快捷导航卡片调整为单行四列，避免第四个栏目单独换行。
- 样式写入项目自定义样式文件，不修改依赖目录中的主题源码。
- 保留移动端原有的单卡滑动导航，避免窄屏内容被压缩。

## 2026-07-24：内容目录与首页治理

### 目录调整

- 将数据管理、数据架构和数据应用统一移动到 `10.数据技术`。
- 将 `50.AIBigModel` 规范为 `20.AI大模型`。
- 新增 `30.项目管理`，保留 `40.编程语言`。
- 项目管理分为预测型项目和敏捷项目；预测型项目按五大过程组组织，敏捷项目按原则、团队、产品、交付和改进组织。
- 文章文件名和已发布 permalink 保持不变，旧 `/dm/`、`/data_structure/`、`/data_app/` 目录入口继续有效。

### 首页与图片

- 首页和导航顺序统一为数据技术、AI大模型、项目管理、编程语言。
- 新增 `/data/` 和 `/project-management/` 入口。
- 使用 AI 生成四张统一风格的 3:2 栏目图片，压缩为 1200×800 JPG，单张约 200–250 KB。
- 图片不包含文字、品牌标志或水印，生成语义与维护方式记录在 `HOMEPAGE_ASSETS.md`。

### 构建兼容处理

第一次生产构建发现旧版 `Catalogue` 组件无法渲染合并数据目录的深层结构。最终将 `/data/` 调整为普通 Markdown 索引，并继续使用三个原目录页承载自动目录树，避免修改主题包或破坏旧链接。

### 验收证据

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 55 个 Markdown、32 篇普通文章、159 个图片引用；0 错误、18 个空标签警告 |
| `npm run build` | 客户端和服务端编译成功，生成 56 个 HTML |
| `npm run check:dist` | 检查 5,564 个站内构建引用，0 错误 |
| 本地开发服务 | 首页、新目录页、旧兼容目录页和四张栏目图片均返回 HTTP 200 |
| 首页生产 HTML | 四个栏目及图片顺序与确认方案一致 |

### 本轮边界

- 未执行 commit、push、deploy、百度推送或 npm 发布。
- 保留原有 18 个空标签警告，未在本次目录治理中扩展内容修订范围。

## 2026-07-24：发布前工程治理

### 背景

项目最初存在开发启动兼容问题、批量图片文件名异常、`node_modules` 被 Git 跟踪、域名配置分裂、明文百度 token 和高风险强制发布脚本。目标是在不迁移框架的前提下恢复可维护、可检查、可本地发布的 GitHub Pages 工程。

### 已执行

- 使用 `npm ci` 恢复依赖，解决缺失模块问题。
- 为 webpack 4 增加 OpenSSL 3 兼容参数，恢复 Node 22 下的开发与构建。
- 校验并恢复 39 个原图片文件名，清除全部时间戳副本。
- 修复 `00_mpp_calc_model.png.` 错链。
- 清除 16 个 `.DS_Store`。
- 停止 Git 跟踪 47,196 个 `node_modules` 文件，本地依赖保留。
- 重整 `.gitignore`。
- 固定 Node 22.22.2、npm 10.9.7，并声明支持范围。
- 统一生产地址、VuePress base、sitemap、GitHub 仓库链接和百度 URL 默认值。
- 删除空 `CNAME`。
- 从当前工作树移除明文百度 token，改为 `BAIDU_TOKEN` 环境变量和 HTTPS 接口。
- 增加 `check`、`check:dist`、`verify`、`deploy:dry-run` 命令。
- 重构 `deploy.sh`，增加仓库、分支、工作区、远端源码一致性和并发发布保护。
- 增加 `.nojekyll` 发布处理。
- 增加只做验证、不自动部署的 GitHub Actions CI。
- 补充项目总览、架构、内容、运维、部署回滚和风险文档。

### 验收证据

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 41 个 Markdown、32 篇文章、158 个图片引用；0 错误、18 个空标签警告 |
| `npm run build` | 客户端和服务端编译成功，生成 42 个 HTML |
| `npm run check:dist` | 检查 4,074 个站内构建引用，0 错误 |
| `npm ci` | 从锁文件重新安装 1,531 个包成功 |
| `npm run deploy:dry-run` | 完整发布演练成功，未连接或推送远端 |
| `npm run dev` | 编译成功；首页、文章页和已恢复图片均返回 HTTP 200 |
| sitemap | 42 个目标 GitHub Pages URL |
| Git 跟踪 node_modules | 0 |
| 时间戳图片副本 | 0 |
| 构建产物 CNAME | 0 |
| 构建产物 `.DS_Store` | 0 |

### 未执行

- 未创建 Git commit。
- 未推送 `main` 或任何其他分支。
- 未执行真实 `npm run deploy`。
- 未执行百度 URL 推送。
- 未执行 npm 主题发布。
- 未重写 Git 历史或轮换百度平台 token。
- 未执行 VuePress/Vue/webpack 大版本迁移。

### 外部只读核对

- 公开 HTTPS 查询确认远端 `main` 为 `4c607228`、`gh-pages` 为 `f13c8e76`。
- 当前线上首页和抽查图片返回 HTTP 200。
- 当前线上 sitemap 仍引用旧自定义域名，需要真实部署本地新构建后更新。
- 完成 SSH 配置后，默认 `ssh -T git@github.com` 和 `git fetch --prune origin` 均验证成功。

### 分支对齐

- 本地 `main` 从 `21f22ad2` 快进到远端 `4c607228`，当前与 `origin/main` 差异为 `0/0`。
- 远端唯一变更是 `package.json` 的 OpenSSL 兼容配置。
- 重叠配置按用户要求保留本地治理版本；未冲突的远端提交历史已纳入本地 `main`。
- 对齐过程未覆盖本地工作区文件，`node_modules` 取消跟踪状态已重新建立。

### 待办

1. 审查本轮工作区 diff。
2. 在百度站长平台轮换旧 token。
3. 确认 GitHub Pages 来源为 `gh-pages /(root)`。
4. 经用户单独确认后提交、推送 `main` 并执行真实部署。
5. 后续补齐 18 篇文章标签，并规划依赖清理与框架迁移。
